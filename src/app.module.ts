import { Module } from "@nestjs/common";

/*
 * AppModule is the application's root composition module. As features are
 * introduced, it will connect their modules, controllers, and providers.
 */
@Module({
  imports: [],
  controllers: [],
  providers: [],
})
export class AppModule {}
